# Configuration commune
APP_ENVIRONMENT=local
# Permet de surcharger la configuration CORS si nécessaire (séparateur par virgule)
# APP_CORS_ALLOWED_ORIGINS=http://localhost:8081,https://votre-service.onrender.com

# --- Base locale -------------------------------------------------------------
# Lancement en local avec docker-compose (PostgreSQL exposé sur 5433)
APP_DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5433/assemblee

# --- Déploiement Render ------------------------------------------------------
# Renseigner l'URL Render (variable RENDER_EXTERNAL_URL fournie automatiquement)
# et la base Postgres managée par Render (DATABASE_URL fournie automatiquement).
# Les valeurs ci-dessous sont des placeholders : remplacez-les avec vos secrets.
# DATABASE_URL=postgresql+psycopg2://render_user:render_pass@render-host:5432/render_db
# RENDER_EXTERNAL_URL=https://votre-service.onrender.com
